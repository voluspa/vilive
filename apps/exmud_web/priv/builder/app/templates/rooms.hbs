<!--
    TODO: ok so doesn't render any more 
    want to reuse the world controller for the world viewer again
    but now control if we are picking location or selected or whatever
    via controller to controller interactions instead of the view passing it in
-->
{{view 'worldviewer' class="room" controller=world}}
{{outlet}}
